import Layout from "../../components/BlogLayout.tsx";

export const meta = {
  title: "Django Rest Framework's Class Based View may not Your Answer"
}

# {meta.title}

this post is about to something

export default ({ children }) => <Layout>{children}</Layout>;
